{
  "profile": "base",
  "description": "Minimal HABLA Observer IA application runtime.",
  "groups": ["base"],
  "requires": ["git", "python", "node", "npm"],
  "commands": ["create .venv", "install backend requirements", "npm install", "npm run build"]
}
