Cliente: planta industrial con camaras USB e IP.

Necesita detectar piezas defectuosas con OpenCV y YOLO, leer codigos QR,
guardar eventos en PostgreSQL, mostrar dashboard web en React, conectar
sensores por serial/MQTT y generar reportes con matplotlib.

La estacion puede tener RTX 4070, pero debe degradar a CPU si no hay CUDA.
