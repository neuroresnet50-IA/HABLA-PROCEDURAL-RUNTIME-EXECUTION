# Validacion local no-bwrap

Este workspace registra una validacion local minima sin depender de `bwrap`.
La evidencia esperada es la existencia de este archivo bajo `docs/` y su
verificacion con un comando local de Python ejecutado desde la raiz del proyecto.
